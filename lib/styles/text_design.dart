import 'package:flutter/material.dart';

const textDesign= TextStyle(
              fontWeight:FontWeight.bold,
              fontSize: 55,
              color: Colors.white,
               shadows: [
                    Shadow(
                      color: Colors.black,
                      blurRadius: 10
                    )
                  ]
            );